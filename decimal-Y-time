function setup() {
  createCanvas(windowWidth, windowHeight);
}
function draw(){

    let hr = hour();
    let mn = minute();
    let sc = second();

    fill(0);
    noStroke();
    text(hr + ':' mn ':' + sc, 10, 200)
}